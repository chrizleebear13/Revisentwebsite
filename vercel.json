{
  "buildCommand": "cd Demo && npm install && npm run build && cp -r dist/* ../demo-dashboard/",
  "outputDirectory": ".",
  "cleanUrls": true,
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/:path*" }
  ]
}
